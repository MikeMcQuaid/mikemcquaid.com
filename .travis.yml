language: ruby
sudo: false
rvm: 2.3.3
cache: bundler
env:
  global: NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of html-proofer
addons:
  apt:
    packages:
    - libcurl4-openssl-dev # required to avoid SSL errors
branches:
  only:
    - master
script: bundle exec rake test
notifications:
  email:
    on_success: never
    on_failure: always

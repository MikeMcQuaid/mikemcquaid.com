#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${repo_root}"

export HOMEBREW_GENERATE_TRANSCRIPTS=1
source "${repo_root}/script/bootstrap"
bundle_bootstrap

bundle exec ruby lib/enrich_metadata_cli.rb "$@"
